steps:
  - name: "gcr.io/cloud-builders/docker"
    args:
      - build
      - -t
      - "gcr.io/$PROJECT_ID/academic-copilot-backend:$COMMIT_SHA"
      - backend/

  - name: "gcr.io/cloud-builders/docker"
    args:
      - push
      - "gcr.io/$PROJECT_ID/academic-copilot-backend:$COMMIT_SHA"

  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    entrypoint: gcloud
    args:
      - run
      - deploy
      - academic-copilot-backend
      - --image=gcr.io/$PROJECT_ID/academic-copilot-backend:$COMMIT_SHA
      - --region=us-central1
      - --platform=managed
      - --allow-unauthenticated
      - --memory=1Gi
      - --timeout=300
      - --max-instances=1
      - --set-secrets=ANTHROPIC_API_KEY=anthropic-api-key:latest
images:
  - "gcr.io/$PROJECT_ID/academic-copilot-backend:$COMMIT_SHA"